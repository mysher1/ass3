name: ass2
description: "SWE311 Assignment 2 - Private Memo (SQLite CRUD)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # SQLite database
  sqflite: ^2.3.3
  path: ^1.9.0

  # Local login state
  shared_preferences: ^2.2.3

  # Password hashing (SHA-256)
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
